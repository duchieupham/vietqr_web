import 'package:flutter/material.dart';

class TransPendingView extends StatelessWidget {
  const TransPendingView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Danh sách giao dịch chờ xác nhận',),
      ],
    );
  }
}
